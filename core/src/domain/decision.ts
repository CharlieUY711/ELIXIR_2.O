/**
 * Decision - Enum cerrado de decisiones del Elixir Core
 * 
 * NO expone razones.
 * NO expone mensajes.
 * NO expone metadata externa.
 */

export enum Decision {
  ALLOW = 'ALLOW',
  DENY = 'DENY'
}

